run:
  timeout: 5m
  tests: false

linters-settings:
  govet:
    check-shadowing: true
  gocyclo:
    min-complexity: 15
  maligned:
    suggest-new: true
  dupl:
    threshold: 100
  goconst:
    min-len: 2
    min-occurrences: 2
  misspell:
    locale: US
  lll:
    line-length: 120
  goimports:
    local-prefixes: github.com/smnsjas/go-psrpcore
  gocritic:
    enabled-tags:
      - diagnostic
      - experimental
      - opinionated
      - performance
      - style
    disabled-checks:
      - wrapperFunc
      - ifElseChain
      - octalLiteral

linters:
  enable:
    - megacheck
    - govet
    - revive
    - gosec
    - gocyclo
    - unconvert
    - goconst
    - gscoped
    - misspell
    - lll
    - unparam
    - typecheck
    - ineffassign
    - staticcheck
    - unused
    - gosimple
  disable:
    - errcheck

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0
